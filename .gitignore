# Dependencias (por si en el futuro se añaden)
node_modules/

# Artefactos generados (se crean con `npm run build`)
dist/
tools/preview.svg

# Sistema operativo / editores
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
